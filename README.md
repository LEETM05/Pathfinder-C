# Navigation Project

## Description
This project appears to be a C application related to navigation. Further details will be added here.

## Files
- `NAVI.c`: Main source file for navigation logic.
- `Navigation.c`: Additional source file for navigation.
- `Navigation.h`: Header file containing declarations.
- `NAVIGATION.csv`: Data file, possibly for navigation points or configuration.
- `Navigation.vcxproj`: Visual Studio project file.

## Build Instructions
This project can be built using Visual Studio. Open `Navigation.vcxproj` and build the solution.

## How to Run
After building, the executable `Navigation.exe` will be generated in the `x64/Debug` (or `x64/Release`) directory.
